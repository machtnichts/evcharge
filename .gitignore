# Live settings - written back by the running app, machine specific.
config.json
config.json.bak

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Run artifacts (the service writes its log next to the project)
*.log
*.stdout

# Recorded session history (live data, never part of the repository)
logs/

# Editor / OS
.DS_Store
*.swp
.idea/
.vscode/

# Local notes that must never be published
NOTES-local.md

# Local-only tools and parked variants (references to the retired third-party
# controller live here, not in the repository)
local-tools/
